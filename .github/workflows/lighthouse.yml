name: Lighthouse CI
on:
  push:
    branches: [main]
    paths:
      - 'web-app/**'
      - '.github/workflows/lighthouse.yml'
  pull_request:
    paths:
      - 'web-app/**'
      - '.github/workflows/lighthouse.yml'
jobs:
  lighthouse:
    name: Lighthouse Audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web-app
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web-app/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Cache build output
        id: build-cache
        uses: actions/cache@v4
        with:
          path: web-app/dist
          key: build-${{ hashFiles('web-app/src/**', 'web-app/public/**', 'web-app/package-lock.json', 'web-app/vite.config.ts', 'web-app/index.html', 'web-app/tsconfig.json', 'docs/api/volleymanager-openapi.yaml') }}
      - name: Cache generated API types
        if: steps.build-cache.outputs.cache-hit != 'true'
        id: api-cache
        uses: actions/cache@v4
        with:
          path: web-app/src/api/schema.ts
          key: api-types-${{ hashFiles('docs/api/volleymanager-openapi.yaml', 'web-app/package-lock.json') }}
      - name: Generate API types
        if: steps.build-cache.outputs.cache-hit != 'true' && steps.api-cache.outputs.cache-hit != 'true'
        run: npm run generate:api
      - name: Build application
        if: steps.build-cache.outputs.cache-hit != 'true'
        run: npm run build
      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          # Use artifacts instead of public storage to avoid exposing internal routes
          uploadArtifacts: true
          # Configuration file contains staticDistDir and other settings
          configPath: ./web-app/lighthouserc.json
