/**
 * Date formatting utilities for mobile app.
 *
 * @deprecated Import from '@volleykit/shared/utils' instead.
 * These re-exports exist for backward compatibility.
 */

import { formatLocalizedDate, formatLocalizedDateTime } from '@volleykit/shared/utils'

/**
 * @deprecated Use `formatLocalizedDate` from '@volleykit/shared/utils' instead.
 */
export const formatDate = formatLocalizedDate

/**
 * @deprecated Use `formatLocalizedDateTime` from '@volleykit/shared/utils' instead.
 */
export const formatDateTime = formatLocalizedDateTime
